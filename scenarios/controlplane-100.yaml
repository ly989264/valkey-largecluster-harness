name: controlplane-100
run_id: controlplane-100
topology_mode: physical_aligned
topology: primary_replica
node_count: 100
replicas_per_primary: 1
validation:
  duration_seconds: 600
  require_full_slot_coverage: true
traffic:
  profile: controlplane
  clients: 8
virtual_az_placement:
  mode: physical_aligned
tests:
  - cluster_create_check
  - failover
timeouts:
  scenario_seconds: 3600
  cluster_convergence_seconds: 600

name: scale-500
run_id: scale-500
topology_mode: physical_aligned
topology: primary_replica
node_count: 500
replicas_per_primary: 1
validation:
  duration_seconds: 1200
  require_full_slot_coverage: true
traffic:
  profile: scale
  clients: 24
virtual_az_placement:
  mode: physical_aligned
tests:
  - cluster_create_check
  - stability_gates
timeouts:
  scenario_seconds: 7200
  sampled_check_seconds: 30
  full_check_seconds: 600
